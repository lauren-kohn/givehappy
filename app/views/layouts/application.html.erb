<!DOCTYPE html>
<html>
  <head>
    <title>GiveHappy</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>

  </head>

  <body>
    <div>
      <!-- needs conditional logged_in? logic -->
      <%= button_to "Log Out", logout_path, method: "delete" %>
    </div>

  <div>
    <% flash.each do |msg| %>
      <%= msg.last %>
    <% end %>
  </div>

    <%= yield %>
  </body>
</html>
